# Lazarus – API Reference

> Auto-generated by `doc/gen.lua`. Do not edit by hand.

---

## Module `src.error`

Error type, formatting, and throw utilities for the Lazarus interpreter.

All runtime errors are represented as `Error` objects so they carry source
position and can render a source-code snippet with a caret pointing at the
offending token.

### class `Error`

**Fields**

| Field | Type |
|-------|------|
| `type` | `string       Error kind constant (see `Error.Type`)` |
| `message` | `string       Human-readable description` |
| `line` | `number | nil Line in the *user's* source where the error occurred` |
| `col` | `number | nil Column in the *user's* source where the error occurred` |
| `source` | `string | nil Full source text — used to render the inline snippet` |
| `span` | `number | nil Number of source characters to underline (default 1)` |

### `Error.new()`

Construct a new Error value. All positional fields are optional; when
`source` is provided the formatted output will include an inline snippet.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `err_type` | `string` |  |
| `message` | `string` |  |
| `line` | `number` | | nil |
| `col` | `number` | | nil |
| `source` | `string` | | nil |
| `span` | `number` | | nil |

**Returns**

- `Error`

### `Error:__tostring()`

**Returns**

- `string`

### `Error.throw()`

Create an Error and immediately raise it via `error()`.  This function
never returns; annotated `---@noreturn` so the type-checker can narrow
types in code that follows a guarded call.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `err_type` | `string` |  |
| `message` | `string` |  |
| `line` | `number` | | nil |
| `col` | `number` | | nil |
| `source` | `string` | | nil |
| `span` | `number` | | nil |

`noreturn`

### `src.error.format_location()`

Format the location portion of the error header (`"unknown:line:col"`).

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `err` | `Error` |  |

**Returns**

- `string`

### `src.error.get_source_line()`

Extract a single line from `source` by 1-based line number.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `source` | `string` |  |
| `target_line` | `number` |  |

**Returns**

- `string` — | nil

### `src.error.format_snippet()`

Build the source-snippet lines (code line + caret line) for `err`.
Returns an empty table when the error lacks position or source text.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `err` | `Error` |  |

**Returns**

- `string[]`

### `Error.format()`

Render `err` as a coloured box string.  Does *not* include the stack
trace; see `Error.traceback` for that.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `err` | `Error` |  |

**Returns**

- `string`

### `Error.traceback()`

Collect Lua stack frames starting at `start_level`, filtering out
internal error-handling frames, and return a formatted traceback string.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `start_level` | `number` | | nil |

**Returns**

- `string`

`nodiscard`

---

## Module `src.frontend.lexer`

Source-text lexer: scans a raw string and produces a flat `Token[]`.

The lexer is a simple single-pass scanner.  It records `line` and `column`
for every token so that the parser can attach accurate source positions to
any errors it raises.

### class `Lexer`

**Fields**

| Field | Type |
|-------|------|
| `source` | `string  Original source text` |
| `pos` | `integer Current byte position (1-based)` |
| `line` | `integer Current line number (1-based)` |
| `col` | `integer Current column number (1-based)` |
| `current` | `string  Character at `pos`, or `""` at EOF` |

### `Lexer.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `source` | `string` |  |

**Returns**

- `Lexer`

### `Lexer:_advance()`

Advance one character, updating `line` and `col`.

### `Lexer:_read_identifier()`

Scan a keyword or identifier starting at the current position.

**Returns**

- `Token`

### `Lexer:_read_number()`

Scan a decimal integer literal starting at the current position.

**Returns**

- `Token`

### `Lexer:_read_symbol()`

Scan a single-character symbol (operator or punctuation).
Throws `UNEXPECTED_CHAR` for characters not in the keyword table.

**Returns**

- `Token`

### `Lexer:_next_token()`

Return the next non-whitespace token, or `nil` at EOF.

**Returns**

- `Token?`

### `Lexer:scan()`

Scan the full source and return all tokens as a flat list.

**Returns**

- `Token[]`

---

## Module `src.frontend.lexer.keywords`

Keyword and operator tables used by the lexer and the statement dispatcher.

### alias `TokenType`

| "LET"
| "ASSIGN"
| "PLUS"
| "MINUS"
| "IDENTIFIER"
| "NUMBER"
| "SYMBOL"
| "MULTIPLY"
| "LEFT_BRACKET"
| "RIGHT_BRACKET"

### `Keywords.is_invalid_token_type()`

Return `true` when `token_type` cannot legally appear in expression
position (i.e. it is a keyword-only or assignment-only token).

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `token_type` | `string` |  |

**Returns**

- `boolean`

---

## Module `src.frontend.lexer.token_builder`

Test-only helpers for building `Token` values without running the lexer.

`PendingToken` is an intermediate form with zero-position that can be
promoted to a real `Token` via `with_position`.

### class `PendingToken`

**Fields**

| Field | Type |
|-------|------|
| `type` | `TokenType` |
| `value` | `string` |
| `literal` | `any` |
| `line` | `integer` |
| `col` | `integer` |

### class `TokenBuilder`

### `TokenBuilder.make()`

Create a zero-position pending token with the given type and value.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `type` | `TokenType` |  |
| `value` | `string` |  |

**Returns**

- `PendingToken`

### `TokenBuilder.with_position()`

Promote a `PendingToken` to a positioned `Token`.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `token` | `PendingToken` |  |
| `line` | `integer` |  |
| `col` | `integer` |  |

**Returns**

- `Token`

### `TokenBuilder.number()`

Construct a NUMBER token, converting `value` to a Lua number for `literal`.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `value` | `string` |  |
| `line` | `integer` |  |
| `col` | `integer` |  |

**Returns**

- `Token`

### `TokenBuilder.identifier()`

Construct a keyword or identifier token.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `type` | `TokenType` |  |
| `value` | `string` |  |
| `line` | `integer` |  |
| `col` | `integer` |  |

**Returns**

- `Token`

---

## Module `src.frontend.lexer.token`

Token produced by the lexer.

Every token carries both the raw source text (`value`) and an optional
pre-converted value (`literal`, e.g. a Lua number for `NUMBER` tokens).
Downstream code should use `literal` for semantic purposes and `value`
only when reproducing the original source text.

### alias `TokenLiteral string | number | boolean | nil`

Allowed concrete types for a token's converted literal value.

### class `Token`

**Fields**

| Field | Type |
|-------|------|
| `type` | `TokenType    Symbolic type constant (e.g. `"NUMBER"`, `"LET"`)` |
| `value` | `string       Raw source text of the token` |
| `line` | `integer      1-based source line` |
| `column` | `integer      1-based source column` |
| `literal` | `TokenLiteral Converted value (e.g. a Lua `number` for `NUMBER` tokens)` |

### `Token.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `type` | `TokenType` |  |
| `value` | `string` |  |
| `line` | `integer` |  |
| `column` | `integer` |  |

**Returns**

- `Token`

### `Token:__tostring()`

**Returns**

- `string`

---

## Module `src.frontend.parser.ast`

Root AST node produced by the parser (`Program`).

### class `AST`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"Program"` |
| `body` | `Stmt[]   Top-level statement list` |

### `AST.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `body` | `Stmt[]` |  |

**Returns**

- `AST`

---

## Module `src.frontend.parser.expressions`

Expression parsing rules for the recursive-descent parser.

Operator precedence (lowest → highest):
  additive       (+  -)
  multiplicative (*)
  primary        (literals, identifiers, grouped expressions)

Each level calls the next-higher level for its operands, which
automatically enforces the correct binding strength.

### `Parser:_expression()`

Entry point — delegates to the lowest-precedence rule.

**Returns**

- `Expr`

### `Parser:_additive()`

Parse additive expressions (`+`, `-`), left-associative.

**Returns**

- `Expr`

### `Parser:_multiplicative()`

Parse multiplicative expressions (`*`), left-associative.

**Returns**

- `Expr`

### `Parser:_primary()`

Parse a primary: a literal, identifier, or parenthesised sub-expression.

**Returns**

- `Expr`

---

## Module `src.frontend.parser`

Recursive-descent parser: consumes a flat token list and produces an AST.

Statement and expression parsing is split into separate modules
(`statements/` and `expressions.lua`) that extend this class by calling
the returned function with the `Parser` table.

### class `Parser`

**Fields**

| Field | Type |
|-------|------|
| `pos` | `integer      Current position in `token_table` (1-based)` |
| `token_table` | `Token[]      Flat list of tokens from the lexer` |
| `source` | `string | nil Original source text, forwarded to errors for snippets` |
| `_statement` | `fun(self: Parser): Stmt` |
| `_expression` | `fun(self: Parser): Expr` |
| `_additive` | `fun(self: Parser): Expr` |
| `_multiplicative` | `fun(self: Parser): Expr` |
| `_primary` | `fun(self: Parser): Expr` |

### `Parser.new()`

Create a new parser over `token_table`.
Pass `source` to enable source-snippet display in error messages.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `token_table` | `Token[]` |  |

**Returns**

- `Parser`

### `Parser:_current()`

Return the token at the current position without consuming it.

**Returns**

- `Token?`

### `Parser:_peek()`

Return the token `offset` positions ahead of current (default 1).
`_peek(0)` is equivalent to `_current()`; `_peek()` is the next token.

**Returns**

- `Token?`

### `Parser:_advance()`

Consume and return the current token, advancing the position.
Throws `UNEXPECTED_EOF` if already at the end of the stream.

**Returns**

- `Token`

### `Parser:_previous()`

Return the most recently consumed token (one position behind current).

**Returns**

- `Token?`

### `Parser:_check()`

Return `true` if the current token has the given type (without consuming).

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `type` | `string` |  |

**Returns**

- `boolean`

### `Parser:_match()`

If the current token matches any of the given types, consume it and
return `true`; otherwise leave the position unchanged and return `false`.

**Returns**

- `boolean`

### `Parser:_consume()`

Consume the current token if it has the expected type.
Throws `SYNTAX_ERROR` with `message` and the token's source position otherwise.

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `type` | `string` |  |
| `message` | `string` |  |

**Returns**

- `Token`

### `Parser:_is_eof()`

Return `true` when all tokens have been consumed.

**Returns**

- `boolean`

### `Parser:parse()`

Parse the full token stream and return the root `AST` (Program) node.

**Returns**

- `AST`

---

## Module `src.frontend.parser.nodes.binary`

AST node for a binary infix operation (e.g. `a + b`, `x * y`).

### class `BinaryExpr` extends `Expr`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"BinaryExpr"` |
| `op` | `string   Token type of the operator (e.g. `"PLUS"`, `"MULTIPLY"`)` |
| `left` | `Expr` |
| `right` | `Expr` |

### `BinaryExpr.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `op` | `string` |  |
| `left` | `Expr` |  |
| `right` | `Expr` |  |

**Returns**

- `BinaryExpr`

---

## Module `src.frontend.parser.nodes.expression_stmt`

AST node that wraps a bare expression used as a statement.

### class `ExpressionStmt` extends `Stmt`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"ExpressionStmt"` |
| `expression` | `Expr` |

### `ExpressionStmt.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `expression` | `Expr` |  |

**Returns**

- `ExpressionStmt`

---

## Module `src.frontend.parser.nodes.identifier`

AST node for a bare identifier reference (e.g. `foo`, `my_var`).

### class `IdentifierExpr` extends `Expr`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"IdentifierExpr"` |
| `name` | `string   Raw identifier text from the source` |

### `IdentifierExpr.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` |  |

**Returns**

- `IdentifierExpr`

---

## Module `src.frontend.parser.nodes.literal`

AST node for a literal value (number or string constant).

### class `LiteralExpr` extends `Expr`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"LiteralExpr"` |
| `kind` | `"number" | "string"  Distinguishes numeric and string literals for the evaluator` |
| `value` | `any                  The converted value (`tonumber` result for numbers)` |

### `LiteralExpr.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `kind` | `"number"` | | "string" |
| `value` | `any` |  |

**Returns**

- `LiteralExpr`

---

## Module `src.frontend.parser.nodes.variable`

AST node for a `let` variable declaration (`let name = expr`).

### class `VariableDecl` extends `Stmt`

**Fields**

| Field | Type |
|-------|------|
| `type` | `"VariableDecl"` |
| `name` | `string     Identifier name` |
| `value` | `Expr | nil Initialiser expression, or `nil` when omitted` |

### `VariableDecl.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `name` | `string` |  |
| `value` | `Expr` | | nil |

**Returns**

- `VariableDecl`

---

## Module `src.frontend.parser.statements`

Statement dispatcher: routes the current token to the appropriate handler.

To add a new statement type, create a `StatementParser` in its own file
and add it to `HANDLERS`.  No other code needs to change.

### `Parser:_statement()`

Parse one statement.

If the current token matches a registered keyword the corresponding
handler is called (with the keyword already consumed).  Unrecognised
keyword-only tokens produce a `SYNTAX_ERROR`.  Anything else is
treated as an expression statement.

**Returns**

- `Stmt`

---

## Module `src.frontend.parser.statements.let`

Statement handler for `let` variable declarations.

Grammar: `let <identifier> [ = <expression> ]`

The `=` and initialiser are optional; omitting them produces a `VariableDecl`
with `value = nil`.

---

## Module `src.frontend.parser.statements.statement_parser`

Abstract interface for keyword-triggered statement parsers.

Each concrete statement (e.g. `let`) returns a `StatementParser` created
with `StatementParser.new`.  The dispatcher in `statements/init.lua`
collects these, builds a lookup table keyed by `keyword`, and calls
`handler.parse(parser)` when the matching token is seen.

**Contract**: when `parse` is called, the keyword token has already been
consumed by the dispatcher.  `parser:_current()` points at the token
immediately after the keyword.

### class `StatementParser`

**Fields**

| Field | Type |
|-------|------|
| `keyword` | `string         Token type that triggers this handler (e.g. `"LET"`)` |
| `parse` | `fun(parser: Parser): Stmt` |

### `StatementParser.new()`

**Parameters**

| Name | Type | Description |
|------|------|-------------|
| `keyword` | `string` |  |
| `parse_fn` | `fun(parser:` | Parser): Stmt |

**Returns**

- `StatementParser`

---

## Module `src.frontend.parser.types`

Base type annotations shared across all AST node modules.

`Expr` and `Stmt` are abstract base classes; concrete node types inherit
from one of these (e.g. `---@class BinaryExpr: Expr`).

### class `Expr`

Base class for all expression nodes.

**Fields**

| Field | Type |
|-------|------|
| `type` | `string  Discriminant tag (e.g. `"BinaryExpr"`, `"LiteralExpr"`)` |

### class `Stmt`

Base class for all statement nodes.

**Fields**

| Field | Type |
|-------|------|
| `type` | `string  Discriminant tag (e.g. `"VariableDecl"`, `"ExpressionStmt"`)` |

---

## Module `src.repl`

### `src.repl.dump()`

### `src.repl.main()`

---

let main = () => {
   let test: number = 5;
   print("hello world!");
   // a comment!

   /*
   multi
   line
   comment
   */
   let test = 1;
   let test2 = 3;
   if (test == test2) {
      print("test -w-");
   }
}

// Frame — the control-flow context of a block: whether we are inside a function,
// a loop, and/or a constructor, so `return` / `break` / `self` placement can be
// judged. Immutable — a nested context derives a fresh Frame.

public in_function
public in_loop
public in_constructor

constructor(in_function: bool, in_loop: bool, in_constructor: bool) {
    .in_function = in_function
    .in_loop = in_loop
    .in_constructor = in_constructor
}

// The frame for a loop body: like this one, but with `break` now legal.
public in_loop_body(): Frame {
    return Frame(.in_function, true, .in_constructor)
}

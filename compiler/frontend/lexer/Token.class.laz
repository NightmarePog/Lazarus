// A lexical token: its `kind` tag, raw `value` text, and 1-based start position.

public kind: str
public value: str
public line: int
public column: int
constructor(kind: str, value: str, line: int, column: int) {
    .kind = kind
    .value = value
    .line = line
    .column = column
}
